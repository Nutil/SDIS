To compile the project run the command:
javac src/com/peer/Peer.java
To run the project run the command without arguments to see which info is needed:
java src/com/peer/Peer

To compile the TestApp run the command:
javac src/com/testApp/TestApp.java
To run the project run the command without arguments to see which info is needed:
java src/com/testApp/TestApp